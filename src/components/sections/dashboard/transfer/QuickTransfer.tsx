import CardContainer from 'components/common/CardContainter';
import SliderComponent from 'components/sections/dashboard/transfer/SliderComponent';

const QuickTransfer = () => {
  return <CardContainer title="Quick Transfer" CardComponent={SliderComponent} />;
};

export default QuickTransfer;
